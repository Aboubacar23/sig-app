{{ form_start(form) }}
<div class="card-header">
    <h4 class="card-title">Modifier une Banque</h4>
</div>
<div class="card-body">
    <div class="row mb-2">
        <div class="col-md-12">{{ form_row(form.libelle)}}</div>
    </div>
</div>
<div class="card-footer text-end">
    <div class="d-flex">
        <a href="{{path('app_banque_index')}}" class="btn btn-secondary btn-sm">Retour</a>
        <button type="submit" class="btn btn-primary btn-sm ms-auto">Modifier</button>
    </div>
</div>    
{{ form_end(form) }}