{% extends 'base.html.twig' %}

{% block title %}Edit Produit{% endblock %}

{% block body %}
<div class="container-xl">
    <div class="page-header d-print-none">
        <div class="row g-2 align-items-center">
            <div class="col">
                <h2 class="page-title">Produit</h2>
            </div>
        </div>
    </div>
</div>
{% set valeur = 0 %}
{% set valeur = produit.prixAchat / produit.taux %}
<div class="page-body">
    <div class="container-xl">
        <div class="row row-cards">
            <div class="col-12">
                <div class="card">
                    <p>{{ include('produit/_form.html.twig', {'button_label': 'Modifier'}) }}</p>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
