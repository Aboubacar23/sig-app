<form method="post" action="{{ path('app_depense_camion_delete', {'id': depense_camion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ depense_camion.id) }}">
    <button class="btn">Delete</button>
</form>
