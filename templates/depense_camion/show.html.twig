{% extends 'base.html.twig' %}

{% block title %}DepenseCamion{% endblock %}

{% block body %}
    <h1>DepenseCamion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ depense_camion.id }}</td>
            </tr>
            <tr>
                <th>Banque</th>
                <td>{{ depense_camion.banque }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ depense_camion.montant }}</td>
            </tr>
            <tr>
                <th>Date_depense</th>
                <td>{{ depense_camion.dateDepense ? depense_camion.dateDepense|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Mode_paiement</th>
                <td>{{ depense_camion.modePaiement }}</td>
            </tr>
            <tr>
                <th>Numero_cheque</th>
                <td>{{ depense_camion.numeroCheque }}</td>
            </tr>
            <tr>
                <th>Ordre_de</th>
                <td>{{ depense_camion.ordreDe }}</td>
            </tr>
            <tr>
                <th>Observation</th>
                <td>{{ depense_camion.observation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_depense_camion_index') }}">back to list</a>

    <a href="{{ path('app_depense_camion_edit', {'id': depense_camion.id}) }}">edit</a>

    {{ include('depense_camion/_delete_form.html.twig') }}
{% endblock %}
