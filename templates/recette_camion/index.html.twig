{% extends 'base.html.twig' %}

{% block title %}RecetteCamion index{% endblock %}

{% block body %}

<div class="content-wrapper">
    <div class="row">
        <div class="col-12 grid-margin stretch-card">
            <div class="card">
                <div class="row">
                    <div class="col-lg-12 grid-margin stretch-card">
                        <div class="card">
                            <div class="card-header">
                                <div class="row">
                                    <div class="col-md-3">
                                        <h4 class="card-title">Liste des recettes camions</h4>
                                    </div>
                                    <div class="col-md-9 text-end">
                                        <a href="#" class="btn btn-secondary btn-sm text-white">Nombres : {{ recette_camions | length}}</a>
                                        <a href="{{ path('app_camion_index') }}" class="btn btn-primary btn-sm text-white">Allez au Camion</a>
                                    </div>
                                </div>
                            </div>
                            <div class="card-body">
                                {% for message in app.flashes('success') %}
                                    <div class="alert alert-success">
                                        {{ message }}
                                    </div>
                                {% endfor %}
                                <div class="table-responsive">
                                    <table class="table table-striped">
                                        <thead class="table-dark">
                                        <tr>
                                            <th width="2">N°</th>
                                            <th>Depart</th>
                                            <th>Date Recette</th>
                                            <th>Destination</th>
                                            <th>Type TC</th>
                                            <th>Montant Transport</th>
                                            <th>Mode Paiement</th>
                                            <th>Banque</th>
                                            <th>Numéro chèque</th>
                                            <th>Observation</th>
                                            <th class="text-center" width="150"></th>
                                        </tr>
                                        </thead>
                                        <tbody>
                                        {% set i = 0 %}
                                        {% set montant = 0 %}
                                        {% for item in recette_camions  %}
                                            {% set montant = montant + item.montantTransport %}
                                            {% set i = i + 1 %}
                                            <tr>
                                                <td>{{ i }}</td>
                                                <td>{{ item.dateRecette ? item.dateRecette|date('d-m-Y') : '' }}</td>
                                                <td>{{ item.depart }}</td>
                                                <td>{{ item.destination }}</td>
                                                <td>{{ item.typeTc }}</td>
                                                <td>{{ item.montantTransport | number_format }}</td>
                                                <td>{{ item.modePaiement }}</td>
                                                <td>{{ item.banque }}</td>
                                                <td>{{ item.numeroCheque }}</td>
                                                <td>{{ item.observation }}</td>
                                                <td>
                                                    <a class="btn btn-danger btn-sm text-white" href="{{ path('app_recette_camion_delete', {'id': item.id}) }}" onclick="return confirm('Voulez-vous confirmer ?')"><i class="mdi mdi-delete mdi-24px"></i></a>
                                                    <a class="btn btn-primary btn-sm" href="{{ path('app_recette_camion_edit', {'id': item.id}) }}"><i class="mdi mdi-pencil-box-outline mdi-24px"></i></a>
                                                </td>
                                            </tr>
                                        {% else  %}
                                            <tr>
                                                <td colspan="12" class="text-center">Pas de recette</td>
                                            </tr>
                                        {% endfor %}
                                        </tbody>
                                        <tfoot >
                                        <tr>
                                            <th colspan="5">Total</th>
                                            <th>{{ montant | number_format }} GNF</th>
                                            <th></th>
                                        </tr>
                                        </tfoot>
                                    </table>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


{#
    <h1>RecetteCamion index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Montant_transport</th>
                <th>Mode_paiement</th>
                <th>Date_recette</th>
                <th>Depart</th>
                <th>Banque</th>
                <th>Numero_cheque</th>
                <th>Destination</th>
                <th>Type_tc</th>
                <th>Observation</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for recette_camion in recette_camions %}
            <tr>
                <td>{{ recette_camion.id }}</td>
                <td>{{ recette_camion.montantTransport }}</td>
                <td>{{ recette_camion.modePaiement }}</td>
                <td>{{ recette_camion.dateRecette ? recette_camion.dateRecette|date('Y-m-d') : '' }}</td>
                <td>{{ recette_camion.depart }}</td>
                <td>{{ recette_camion.banque }}</td>
                <td>{{ recette_camion.numeroCheque }}</td>
                <td>{{ recette_camion.destination }}</td>
                <td>{{ recette_camion.typeTc }}</td>
                <td>{{ recette_camion.observation }}</td>
                <td>
                    <a href="{{ path('app_recette_camion_show', {'id': recette_camion.id}) }}">show</a>
                    <a href="{{ path('app_recette_camion_edit', {'id': recette_camion.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="11">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
#}
{% endblock %}
