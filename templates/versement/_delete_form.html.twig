<form method="post" action="{{ path('app_versement_delete', {'id': versement.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ versement.id) }}">
    <button class="btn btn-danger btn-sm">Supprimer <i class="fa fa-trash-o"></i></button>
</form>
