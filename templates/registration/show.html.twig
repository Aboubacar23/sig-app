{% extends 'base.html.twig' %}

{% block title %}Produit{% endblock %}

{% block body %}
<div class="content-wrapper">
    <div class="row">
        <div class="col-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-header">
                    <h4 class="card-title">Détails un Administrateur</h4>
                </div>
                <div class="card-body">
                    <div class="row mb-2">
                        <div class="col-md-9">
                            <table class="table">
                                <tbody>
                                <tr>
                                    <th>Nom</th>
                                    <td>{{ user.nom }}</td>
                                    <th>Prénom</th>
                                    <td>{{ user.prenom }}</td>
                                </tr>
                                <tr>
                                    <th>Email</th>
                                    <td>{{ user.email }}</td>
                                    <th>Telephone</th>
                                    <td>{{ user.tel }}</td>
                                </tr>
                                <tr>
                                    <th>Pays</th>
                                    <td>{{ user.pays }}</td>
                                    <th>Ville</th>
                                    <td>{{ user.ville }}</td>
                                </tr>
                                </tbody>
                            </table>
                        </div>
                        <div class="col-md-3">
                                <span>
                                    {% if user.image == '' %}
                                        <img src="{{asset('images/images/user.png')}}" height="200" width="400">
                                    {% else %}
                                        <img src="{{asset('images/employees/'~ user.image)}}" height="200" width="400">
                                    {% endif %}
                                </span>
                        </div>
                    </div>
                </div>
                <div class="card-footer text-end">
                    <div class="d-flex">
                        <a href="{{path('app_user_edit', {'id' : user.id })}}" class="btn btn-warning btn-sm m-2 text-white">Modifier <i class="mdi mdi-pencil-box-outline "></i></a>
                        <a href="{{path('app_user_delete', {'id' : user.id })}}" class="btn btn-danger btn-sm m-2 text-white" onclick="return confirm('Voulez-vous confirmer ?')">Supprimer <i class="mdi mdi-delete"></i></a>
                        <a href="{{path('app_user_index')}}" class="btn btn-secondary btn-sm m-2 ms-auto text-white">Retour <i class="mdi mdi-reply"></i></a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
