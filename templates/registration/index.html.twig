{% extends 'base.html.twig' %}

{% block title %}admin index{% endblock %}

{% block body %}
<div class="content-wrapper">
    <div class="row">
        <div class="col-12 grid-margin stretch-card">
            <div class="card">
                <div class="row">
                    <div class="col-lg-12 grid-margin stretch-card">
                        <div class="card">
                            <div class="card-header">

                                <div class="row">
                                    <div class="col-md-3">
                                        <h4 class="card-title">Liste des administrateurs</h4>
                                    </div>
                                    <div class="col-md-9 text-end">
                                        <a href="{{path('app_register')}}" class="btn btn-primary btn-sm"><i class="mdi mdi-plus-circle icon-table"></i></a>
                                    </div>
                                </div>
                            </div>
                            <div class="card-body">
                                {% for message in app.flashes('success') %}
                                    <div class="alert alert-success">
                                        {{ message }}
                                    </div>
                                {% endfor %}
                                <div class="table-responsive table-scroll" data-mdb-perfect-scrollbar="true">
                                    <table class="table table-striped mb-0">
                                        <thead>
                                            <tr>
                                                <th scope="col">N°</th>
                                                <th scope="col">Images</th>
                                                <th scope="col">Username</th>
                                                <th scope="col">Nom</th>
                                                <th scope="col">Prenom</th>
                                                <th scope="col">Email</th>
                                                <th scope="col">Tél</th>
                                                <th scope="col">Roles</th>
                                                <th scope="col">Ville</th>
                                                <th scope="col">Pays</th>
                                                <th scope="col" class="text-center" width="100"></th>
                                            </tr>
                                        </thead>
                                        <tbody>

                                        {% set i = 0 %}
                                        {% for user in users %}
                                            {% set i = i + 1 %}
                                            <tr>
                                                <td>{{ i }}</td>
                                                <td>
                                                    {% if user.image == '' %}
                                                        <img src="{{asset('images/images/user.png')}}" height="50" width="50">
                                                    {% else %}
                                                        <img src="{{asset('images/employees/'~ user.image)}}" height="50" width="50">
                                                    {% endif %}
                                                </td>
                                                <td>{{ user.username }}</td>
                                                <td>{{ user.nom }}</td>
                                                <td>{{ user.prenom }}</td>
                                                <td>{{ user.email }}</td>
                                                <td>{{ user.tel }}</td>
                                                <td>
                                                    {% for role in user.roles %}
                                                        {% if role == "ROLE_SUPER_ADMIN" %}
                                                            Super Admin
                                                        {% endif %}
                                                    {% endfor %}
                                                </td>
                                                <td>{{ user.ville }}</td>
                                                <td>{{ user.pays | country_name }}</td>
                                                <td class="text-center">
                                                    <a class="btn-sm btn btn-success text-white btn-sm" href="{{path('app_user_show', {'id' : user.id})}}">Détails<i class="mdi mdi-eye p-1"></i></a>
                                                    <a class="btn-sm btn btn-primary btn-sm" href="{{path('app_user_edit', {'id' : user.id})}}">Modifier <i class="mdi mdi-pencil-box-outline p-1"></i></a>
                                                </td>
                                            </tr>
                                        {% else %}
                                            <tr>
                                                <td colspan="10">Pas d'administrateur </td>
                                            </tr>
                                        {% endfor %}
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
